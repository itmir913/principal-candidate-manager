{
  "name": "principal-candidate-manager",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "node setup.js",
    "dev":   "concurrently \"cargo watch -x 'run --features dev'\" \"wait-on -t 180000 http://localhost:8080/api/health && npm --prefix frontend run dev\"",
    "test":  "cargo test",
    "build": "npm --prefix frontend run build && cargo build --release"
  },
  "devDependencies": {
    "concurrently": "^10.0.0",
    "wait-on": "^8.0.0"
  }
}
