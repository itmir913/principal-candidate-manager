# PCM — 학교장추천자 선발 관리 시스템

한국 고등학교 학교장추천전형 점수·순위·추천 관리 시스템. 내부망(LAN) 단일 exe 배포.

**스택**: Rust + Axum + SQLx + SQLite / Vue 3 + Vite + Tailwind CSS v4  
**스키마**: **출시 후 — 배포된 버전(v1)의 조각 파일은 동결.** 스키마를 바꾸려면 `migrations/v2/` 조각을 새로 만들고 `SCHEMA_VERSION`을 올린다. 실행 순서는 `src/db.rs`의 `V{N}_FRAGMENTS` 배열이 결정 — 새 조각 추가 시 배열에도 등록. `tests/schema_freeze.rs`가 버전별 지문을 대조해 무단 변경을 막는다  
**커밋**: 반드시 GPG 서명 (`--no-gpg-sign` 우회 금지)  
**테스트**: `cargo run` / `npm run dev` 금지 — 검증은 테스트 코드로

---

## 절대 규칙

**1. Float-Free**: 모든 점수·측정값은 ×100000 정수로 DB 저장. 프론트엔드에서 ÷100000 수동 계산 금지 — `Score` newtype이 자동 처리.

**2. Fail-Fast**: 점수 계산 오류는 즉시 `Err` 반환. `unwrap_or(0)` / `unwrap_or_default()` 등 silent fallback 전면 금지. 허용 예외는 `src/docs/silent_fallback_allowed.md`에 명시된 위치만.

**3. 점수 계산은 백엔드 전담**: 프론트엔드는 표시만. 점수 미리보기도 API 호출.

**4. Import는 All-or-Nothing**: 오류 하나라도 rollback + 422. 부분 저장 없음. 중복 행은 error (warning 아님). 유일한 예외: 외부 석차연명부의 **석차 값** 누락·변환 실패는 행 skip + warning (`src/docs/08_excel_import.md` §7-1).

**5. Excel 파싱은 헤더 이름 기반**: 열 인덱스(`cols[0]`) 직접 참조 금지. `excel::col_map` + `require_cols` 사용.

**6. 다중 쓰기는 트랜잭션**: DELETE+INSERT, 루프 INSERT/UPDATE는 반드시 tx. `find_or_create_track`은 항상 `&mut *tx` 전달 (pool 직접 전달 금지).

**7. base_data bulk delete는 student_type 필터 필수**: 재학생 업로드가 졸업생 데이터를 지우면 점수 계산 실패.

**8. 폰트 최소 `text-base`**: `text-sm` / `text-xs` / `font-size: 14px` 이하 금지.
유일한 예외는 **본문이 아닌 배지·pill 라벨**(예: `AdminView.vue` 사이드바 "NEW" 배지) —
읽는 텍스트가 아니라 표식이므로 `text-xs`를 허용한다. 예외 사용 시 해당 줄에 주석을 남긴다.

**9. 문서·주석은 코드보다 약하게 쓴다**: 테스트 개수·함수 개수·변이 건수·경로 수처럼
**세어서 얻은 값**은 커밋 한 번에 낡는다. 낡은 수치는 아무도 못 알아채고, 그대로 인용되어
틀린 주장이 된다(`src/docs/12_verification.md` 가 이걸로 세 번 틀렸다).
대신 **재현 명령**을 적는다 — `cargo test --all`, `npm --prefix frontend test`.
"세는 명령을 괄호에 함께 남기기"도 부족하다. 본문의 수치 자체가 낡기 때문이다.
정도를 말해야 하면 말로 쓴다("전량", "대부분", "일부", "없음").
**예외는 설계가 정한 상수** — ×100000, 소수 5자리, 비밀번호 4자 같은 것은 측정값이
아니라 사양이므로 적는다. 커밋 메시지·이슈처럼 **시점이 박힌 기록**에는 적어도 된다.
**줄 번호 인용도 같다** — `rounds.rs:264` 대신 `rounds.rs` 의 `reopen_round` 라고 쓴다.

같은 이유로 **주석이 코드보다 강하게 주장하지 않게 한다.** 낡은 수치가 위험한 것은
숫자여서가 아니라 **검증되지 않은 단정**이어서다. 주석은 그 단정을 문장으로 할 뿐이고,
유창할수록 더 오래 믿긴다. 실제로 이 저장소에서 나온 것들:

- "소스 개수와 **이 목록의 길이**를 대조한다" — 코드는 별도 하드코딩 표와 비교하고
  있었고, 존재하지도 않는 이름(`DANGER_SITES`)을 가리켰다.
- "위 CHECK 를 **행동으로 다시 확인한다**" — 픽스처가 그 경로에 닿지 못해 CHECK 가
  있든 없든 통과하는 테스트였다.
- "**로직 변경 없이** 옮겼다" — 옮기면서 상한 검사가 하나 추가돼 있었다.
- "**고쳤다**" — 두 검사 중 하나만 고쳤고 나머지는 그대로였다.

**쓰는 법**: ① 주석이 말하는 것을 **코드가 실제로 하는지** 확인하고 쓴다.
② 확인하지 않은 것은 "…로 보인다", "확인하지 않았다"라고 적는다 — 모르는 것을
적는 게 틀리게 적는 것보다 낫다. ③ 테스트 주석에는 **판별력의 소재**를 적는다.
기록용이면 "기록이지 방어선이 아니다"라고 밝힌다.
④ 고쳤다고 쓰기 전에 **변이로 확인한다**(`src/docs/12_verification.md` §1).

