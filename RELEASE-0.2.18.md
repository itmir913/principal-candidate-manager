# 🎉 학교장 추천자 선발 관리 시스템
## v0.2.18 · Public Preview

본 프로그램은 학교 내부망(LAN) 환경에서 동작하는 학교장 추천자 선발 관리 시스템입니다. 단일 실행 파일(.exe)만 실행하면 시스템 트레이에 상주하며, 별도의 서버 구축이나 복잡한 설치 과정 없이 관리자와 담임 교사가 동시에 접속하여 사용할 수 있습니다.

이번 버전은 **v0.2.13 이후 다섯 판(v0.2.14 ~ v0.2.18)의 변경을 한 번에 담은 배포**입니다. 실제 학교 현장에서 선발을 운영하며 나온 요청과 오류 제보를 반영했으며, 담임 교사가 쓰는 화면과 결과 활용 방식이 크게 개선되었습니다.

[공식 홈페이지](https://luminousky.com/teacher-utility-kit/principal-candidate-manager/)에서 예제 템플릿을 다운로드받아 직접 작동시켜보면 프로그램이 어떻게 작동하는지 쉽게 확인할 수 있습니다.

---

## 🚀 v0.2.18 주요 변경 사항

**이번 버전은 기능 추가와 화면 개선이 함께 있는 업데이트입니다.** 특히 **인원 제한이 있는 대학과 없는 대학을 두 개의 프로그램으로 나누어 운영**하는 방식이 정식으로 지원됩니다.

### ✨ 프로그램에 학교 이름을 붙일 수 있습니다

관리자 사이드바에 **설정** 탭이 새로 생겼습니다. 프로그램 제목과 설명을 지정하면 로그인 화면, 사이드바, 브라우저 탭, 담임 교사 화면에 함께 표시됩니다.

- 제목 예: `○○고등학교 학교장추천` / 설명 예: `인원 제한 없는 대학`
- 지정하지 않으면 지금까지와 같은 문구가 표시되므로 **건너뛰어도 됩니다**
- 작업 표시줄 아이콘에 마우스를 올리면 제목과 포트 번호가 표시됩니다 → *프로그램을 두 개 띄웠을 때 어느 창인지 구분*

### ✨ 프로그램 두 개를 동시에 운영할 수 있습니다

인원 제한이 있는 대학과 없는 대학은 관리 방식이 달라, 한 프로그램에 섞으면 전형요소 설정이 서로 발목을 잡습니다. 서로 다른 폴더에 두 벌 설치하고 `pcm/config.json`의 포트를 **8080 / 8081**로 나누면 각각 독립적으로 운영됩니다.

- **인원 제한이 있는 대학**(8080) — 전형요소를 모두 등록해 순위가 정확하게 산출되도록 설정합니다.
- **인원 제한이 없는 대학**(8081) — 대학·모집단위와 내신 정도만 설정합니다. 정원 경쟁이 없으므로 담임 교사는 **학생이 어느 대학에 지원하는지만 입력**하면 됩니다.

두 프로그램은 각자의 `pcm` 폴더에 데이터를 따로 저장하며, 부팅 시 자동 실행도 각각 등록됩니다. 자세한 설정 방법은 **매뉴얼**에 새로 수록했습니다.

### ✨ 담임 교사가 라운드 결과를 CSV로 내려받습니다

담임 교사 **[라운드 결과]** 화면에 `이 라운드 CSV`, `전체 결과 CSV` 버튼이 생겼습니다. **문자 일괄 발송 사이트에 그대로 올릴 수 있는 형식**입니다.

- **학생 한 명이 한 행**입니다. 여러 대학에 지원했으면 선발·미선발을 한 칸에 모아 적습니다.
- 열은 학생코드·학년·반·번호·이름·선발결과입니다. → *총점과 순위는 담지 않습니다. 문자로 나갈 파일이라 치환 문구에 섞이면 그대로 학생에게 전송되기 때문입니다*
- Excel로 열어도 한글이 깨지지 않습니다.

### 🖥 화면 개선

**담임 교사 화면**

- 라운드 결과 표에서 **헤더 반복이 사라졌습니다.** 학생마다 표를 따로 만들던 것을 라운드마다 하나로 합쳤습니다. → *반이 30명이면 같은 헤더를 30번 지나야 했습니다*
- **제목 줄과 표 머리글이 상단에 고정됩니다.** 지원자가 많아도 어느 라운드·어느 열인지 보면서 내려갈 수 있으며, 명단은 잘리지 않고 그대로 이어집니다.
- 학생이 바뀌는 지점이 눈에 띄도록 구분 행을 진하게 하고, 마우스를 올리면 행이 표시됩니다.
- 세 화면(학급 관리·지원자 등록·라운드 결과) 위에 지금 접속한 프로그램 이름이 표시됩니다.

**관리자 화면**

- **대학 설정이 카드 목록이 되었습니다.** 대학마다 카드 한 장에 정원·추천인원·잔여인원과 모집단위 목록이 함께 담깁니다. 대학명·모집단위명 **검색**이 추가되었고, 화면이 넓으면 2열로 배치됩니다. → *예전에는 대학을 하나씩 눌러야 모집단위가 보였습니다*
- 업데이트 & 백업 탭에서 **백업·복원을 맨 위로** 올렸습니다.
- 제작자·라이선스 정보(About)가 설정 탭으로 옮겨졌습니다.
- 감사 기록 메뉴가 설정·매뉴얼과 같은 아래 묶음으로 옮겨졌습니다.

### 🐞 오류 수정

- **졸업생 담당 계정의 "입력 마감"이 실패하던 문제** — 졸업생 담당(0학년 0반)이 입력 마감을 누르면 오류가 발생했습니다. 이제 정상 동작하며, 관리자 라운드 화면의 확정 현황에도 졸업생 담당이 표시됩니다.
- **프로그램 두 개를 함께 쓸 때 자동 실행이 하나만 남던 문제** — 부팅 시 자동 실행 등록이 마지막에 켠 프로그램으로 덮어써졌습니다. 이제 프로그램마다 따로 등록됩니다.
- **미확정 학급 경고를 놓치던 문제** — 라운드 종료 시 "아직 입력을 확정하지 않은 학급이 있습니다" 안내가 다른 문장에 섞여 눈에 띄지 않았습니다. 붉은 경고 상자로 분리했습니다.
- **감사 기록의 영문 표시** — "프로그램 제목 변경"이 영문 원문으로 뜨고 검색 목록에서도 빠져 있었습니다.
- **감사 기록 표의 글자 겹침** — 시각·행위자·행위 열이 좁아 글자가 옆 칸을 침범했습니다.

### 📖 매뉴얼

- 표지 버전·발행일을 현재 판으로 갱신했습니다.
- **설정 탭**과 **담임용 결과 CSV**를 새로 수록했습니다. → *최근 판에서 추가된 기능인데 매뉴얼에 설명이 없었습니다*
- **두 프로그램 동시 운영** 안내를 상세 매뉴얼과 관리자 화면 매뉴얼 양쪽에 넣었습니다.
- 대학 설정 절을 카드·검색 구조에 맞게 고치고, 라운드 마감 절에 담임 화면의 행 색 의미를 적었습니다.

### 기타

- 안정성 개선, 회귀 테스트 34건 추가(총 837건)
- AI(Fable, Opus)를 활용한 코드 감사 4회 — **치명적 결함 0건**, 중대 2건 수정

---

## ⚠️ 업그레이드 전 확인

**데이터는 그대로 사용됩니다.** 데이터베이스 구조 변경이 없으므로 기존 `pcm/data.db`를 그대로 두고 `.exe`만 교체하시면 됩니다. 별도의 마이그레이션이나 재입력은 필요하지 않습니다.

다만 화면과 동작이 여러 곳 바뀌었습니다. **고장이 아니라 의도된 변경**이므로 아래를 먼저 확인해 주십시오.

- **프로그램을 두 개 사용 중이라면, 각각 한 번씩 실행해 주십시오.** 자동 실행 등록 방식이 프로그램마다 분리되도록 바뀌었으며, 각 프로그램이 처음 실행될 때 자기 등록을 새 방식으로 옮깁니다. → *한쪽만 실행하면 그쪽만 이관됩니다*
- **대학 설정 탭의 조작 방식이 바뀝니다.** 대학을 클릭해 선택하는 단계가 없어지고, 모든 대학의 모집단위가 한 화면에 펼쳐집니다. 대학이 많으면 위쪽 **검색칸**을 사용하십시오.
- **라운드 [결과] 탭이 대학 전체 순위로 열립니다.** 모집단위별 순위는 버튼으로 전환합니다. → *관리자와 담임 화면의 기준을 통일했습니다*
- **담임 화면 결과 표에서 노란색 배경이 사라집니다.** 노란색은 관리자가 추천을 고르는 동안 표시되는 "동점" 표식이라 결과가 확정된 담임 화면에는 의미가 없었습니다. 이제 **초록(추천 확정)** 과 **붉은색(미선발·포기)** 만 표시됩니다.
- **관리자 화면의 "학번" 표기가 "학생코드"로 바뀝니다.** 값은 그대로이며 명칭만 통일했습니다.
- **학급 목록 엑셀 내보내기에 0학년 0반 행이 있었다면 더 이상 나오지 않습니다.** 졸업생 담당은 학급이 아니라 별도 항목입니다.

> 선발이 진행 중이라면, 담임 교사가 안내 없이 바뀐 화면을 마주치지 않도록 **라운드 사이나 마감 이후**에 교체하시길 권합니다.

---

## 📥 설치 방법

1. Assets에서 `principal-candidate-manager-0.2.18-windows-x64.zip` 파일을 다운로드합니다.
2. 원하는 위치(예: 바탕화면)에 압축을 해제합니다.
3. `principal-candidate-manager.exe`를 실행합니다.
4. 시스템 트레이의 **학교 아이콘**을 클릭한 뒤 **열기**를 선택합니다.

> 모든 데이터는 실행 파일과 같은 폴더의 `pcm/data.db`에 저장됩니다. 프로그램 업데이트를 진행하려면, 먼저 시스템 트레이에서 `종료`를 선택하여 프로그램을 종료한 후, `.exe` 파일만 최신 버전으로 교체하십시오.

**프로그램 두 개를 함께 운영하려면**, 서로 다른 폴더에 각각 압축을 해제한 뒤 각 폴더의 `pcm/config.json`에서 `"port"` 값을 다르게 지정하십시오(예: 8080 / 8081). 포트가 같으면 나중에 실행한 프로그램이 시작되지 않습니다.

---

## 📌 라이선스

본 프로그램은 공익적 목적의 학교 교육을 지원하기 위해 개발되었으며, **교육 및 비상업적 목적**에 한하여 자유롭게 사용할 수 있습니다.

---

## 💬 피드백

버그 제보와 기능 제안은 **hello@luminousky.com**으로 보내주시면 적극적으로 검토하여 반영하겠습니다.
