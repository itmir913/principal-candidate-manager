# 변경 이력

이 파일은 **0.2.13부터** 관리한다. 그 이전 버전의 변경 내역은 git 로그와
[GitHub 릴리스](https://github.com/itmir913/principal-candidate-manager/releases)를 참고한다.

`publish.yml`은 **초안(draft) 릴리스**를 만든다. 배포할 때 이 파일의 해당 버전 절을
릴리스 본문에 붙여 넣고 공개한다.

---

## 0.2.20

마감된 라운드에서도 **학과명**을 고칠 수 있게 한다. **DB 스키마가 v2로 자동
업그레이드되며, 업그레이드된 DB는 이전 버전 프로그램에서 열 수 없다.**

### 추가

- **마감 후 학과명 수정** — 학과명은 점수 산출에 쓰이지 않고 명단 관리에만 필요한
  값인데, 라운드를 마감하고 나면 오타 하나도 고칠 수 없었습니다. 이제 마감된
  라운드에서도 학과명을 수정할 수 있습니다.
  - 관리자: [라운드] 탭의 지원자 표와 결과 표에서 학과명을 눌러 그 자리에서 고칩니다.
    라운드 상태와 관계없이 언제든 고칠 수 있습니다.
  - 담임교사: [라운드 결과] 화면에서 학과명을 눌러 고칩니다. **최종 마감된 라운드만**
    해당합니다. 진행 중인 라운드는 종전대로 [지원자 등록] 탭에서 지원을 다시 저장하며
    고치고, 마감 처리 중인 라운드는 관리자에게 요청하세요.
  - 수정 내역은 감사 기록에 `학과: 기계공학과 → 전기공학과` 형태로 남습니다.
- **대학·모집단위(계열)는 마감 후에도 바꿀 수 없습니다.** 선발 단위 자체이기
  때문입니다. 점수·순위·추천 결과도 학과명 수정으로 달라지지 않습니다.

### 내부

- 의존성 `joi`를 18.2.8로 올려 보안 경보 2건(낮음)을 닫았습니다. 개발 도구에만
  쓰이는 패키지라 배포본에는 포함되지 않습니다.

### 업그레이드 직후 달라지는 것

- **DB 스키마가 v2로 자동 업그레이드됩니다.** 프로그램을 처음 실행할 때 한 번 일어나며
  별도 조작은 필요 없습니다.
- **되돌릴 수 없습니다.** v2로 올라간 DB를 0.2.19 이하 프로그램으로 열면 "데이터베이스
  스키마 버전이 이 앱이 지원하는 최대 버전보다 높습니다" 안내와 함께 실행이 중단됩니다.
  업그레이드 전에 DB 백업을 받아 두시기 바랍니다.
- 기존 데이터는 그대로 유지됩니다. 점수·순위·추천 결과를 다시 계산하지 않습니다.

---

## 0.2.19

트레이 아이콘 표시 하나를 고친다. 백엔드·스키마·화면 무변경.

### 수정

- **작업 표시줄 아이콘에 프로그램 이름이 보이지 않던 문제** — 제목을 툴팁에만 넣어 두었는데,
  Windows 트레이 툴팁은 마우스를 올려야 뜨고 표시되지 않는 경우가 있습니다. 아이콘을 누르면
  나오는 메뉴 맨 위에 제목·설명·포트 번호를 함께 표시합니다. 프로그램을 두 개 운영할 때
  어느 아이콘이 어느 쪽인지 가리는 것이 이 기능의 목적이라, 툴팁 하나에 맡기지 않습니다.
- 트레이 표시에 **설명(desc)** 이 빠져 있던 것도 함께 고쳤습니다.

### 업그레이드 직후 달라지는 것

- 트레이 아이콘을 누르면 메뉴 맨 위에 제목·설명·포트가 표시됩니다(누를 수 없는 항목).
  제목을 바꾸면 프로그램을 다시 시작한 뒤 반영됩니다

---

## 0.2.18

담임 화면 마무리와 **매뉴얼 갱신**이다. 백엔드·스키마는 무변경
(`SCHEMA_VERSION = 1`, `migrations/`·`src/`·`tests/` 그대로).

### 새로 생긴 것

- **매뉴얼에 "두 프로그램 동시 운영" 안내** — 인원 제한이 있는 대학과 없는 대학은 관리
  방식이 달라 한 프로그램에 섞으면 전형요소 설정이 서로 발목을 잡습니다. 서로 다른 폴더에
  두 벌 설치하고 `pcm/config.json` 의 포트를 8080 / 8081 로 나누면 동시에 운영할 수 있습니다.
  - 인원 제한이 있는 대학(8080)은 전형요소를 모두 등록해 순위가 정확히 나오게 하고,
    없는 대학(8081)은 대학·모집단위와 내신 정도만 설정합니다. 담임교사는 학생이 어느 대학에
    지원하는지만 입력하면 됩니다
  - 상세 매뉴얼 FAQ 와 관리자 화면 매뉴얼 탭 양쪽에 넣었습니다

### 수정

- **담임 [라운드 결과] 화면의 제목 줄과 표 머리글이 상단에 고정됩니다.** 지원자가 많아도
  어느 대학·어느 열인지 보면서 내려갈 수 있습니다. 명단은 잘리지 않고 그대로 이어집니다.
  화면이 좁아 표가 가로로 잘릴 때는 고정하지 않습니다
- **담임 [라운드 결과]도 대학 전체 순위로 열립니다.** 관리자 화면과 기준이 달라, 같은
  학생의 순위가 담임과 관리자에게 다른 수로 보이던 것을 맞췄습니다
- **CSV 내려받기 버튼이 초록색**이 됐습니다. 관리자 화면의 다운로드 버튼과 같은 색입니다

### 매뉴얼

- 표지 버전·발행일을 현재 판으로 갱신(v0.2.12 / 7월 24일 → v0.2.18 / 9월 9일)
- **설정 탭**(프로그램 제목·설명)과 **담임용 결과 CSV** 를 매뉴얼에 새로 넣었습니다.
  둘 다 최근 판에서 생긴 기능인데 매뉴얼에 설명이 없었습니다
- 대학 설정 절을 카드·검색 구조에 맞게 고치고, 추천 확정 절에 두 가지 순위 보기를 적었습니다
- 라운드 마감 절에 담임 화면의 행 색(초록=추천 확정, 붉은색=미선발·포기)을 적었습니다
- 설치 안내의 인쇄 페이지 나눔 위치 조정

---

## 0.2.17

**대학 설정 화면을 카드 목록으로 바꿨다.** 백엔드와 스키마는 건드리지 않았다 —
`src/`·`migrations/`·`tests/` 무변경, 화면 파일 하나만 바뀐다.

### 화면 정리

- **대학 설정 탭이 카드 목록이 됐다.** 예전에는 왼쪽 대학 목록에서 대학을 하나씩 눌러야
  오른쪽에 모집단위가 보였다. 대학당 모집단위가 2~3개뿐이라 오른쪽은 대부분 빈 공간이었다.
  이제 대학마다 카드 한 장에 정원·추천인원·잔여인원과 모집단위 표가 함께 담긴다.
  - **검색**이 생겼다. 대학명뿐 아니라 모집단위명으로도 찾는다
  - 화면이 넓으면 카드가 **2열**로 배치된다. 사이드바를 접거나 창을 반으로 나눠 써도
    표가 잘리지 않도록, 창 크기가 아니라 실제 표시 영역의 폭을 보고 판단한다
  - 같은 줄의 카드는 높이를 맞추고, 동작 버튼은 대학명 길이와 무관하게 같은 자리에 온다

기능은 그대로다 — 대학·모집단위 추가·편집·삭제, 추천 확정 목록 보기, 대학별 명단·정원 현황
내보내기, 대학 설정 가져오기, 재학생 우선 일괄 변경 확인.

### 개발자용

- 모집단위를 대학마다 따로 조회하던 것을 `GET /api/univ-tracks` 한 번으로 바꿨다.
  카드로 펼치면 대학 수만큼 요청이 늘어날 구조였는데, 이 엔드포인트는 이미 있었다
- 대학 id → 모집단위를 computed Map 으로 한 번만 묶는다. 카드마다 전체 목록을 훑으면
  검색어 한 글자마다 대학 수 × 모집단위 수만큼 반복된다
- 2열 전환 기준(1456px)은 표 폭에서 역산했다 — 고정 열 합 552 + 이름 168 = 720, × 2 + 간격

### 업그레이드 직후 달라지는 것

- 대학 설정 탭의 조작 방식이 바뀐다. 대학을 클릭해 선택하는 단계가 없어지고, 모든 대학의
  모집단위가 한 화면에 펼쳐진다

---

## 0.2.16

담임 화면 중심의 개선이다. 라운드 결과를 **문자 발송용 CSV**로 내려받을 수 있고
([#24](https://github.com/itmir913/principal-candidate-manager/issues/24)),
결과 표가 읽기 쉬워졌다
([#29](https://github.com/itmir913/principal-candidate-manager/issues/29)).

스키마는 **동결 상태 그대로**다(`SCHEMA_VERSION = 1`, `migrations/` 무변경). API는 CSV
경로 2개가 **추가**됐을 뿐 제거·개명은 없다.

### 새로 생긴 것

- **라운드 결과 CSV 내려받기** — 담임 화면 [라운드 결과]에 "이 라운드 CSV"와 "전체 결과 CSV"
  버튼이 생겼다. 문자 일괄발송 사이트에 그대로 올릴 수 있도록 **학생 한 명이 한 행**이고,
  여러 대학에 지원했으면 선발·미선발을 한 칸에 모아 적는다.
  - 열은 학생코드·학년·반·번호·이름·선발결과. 총점과 순위는 담지 않는다 — 문자로 나갈
    파일이라 치환 문구에 섞이면 그대로 학생에게 전송된다
  - Excel 로 열어도 한글이 깨지지 않는다

### 수정

- **결과 표에서 헤더가 학생 수만큼 반복되던 문제** — 라운드마다 표 하나로 합쳤다. 학생이
  바뀌는 지점이 눈에 띄도록 구분 행을 진하게 했고, 마우스를 올리면 행이 표시된다.
- **결과 표의 노란색 배경** — 담임 화면에서 미선발 학생 일부만 노란색으로 보였다. 노란색은
  관리자가 추천을 고르는 동안 "동점이니 유의하라"는 표시라 결과가 확정된 화면에는 뜻이 없다.
  이제 추천 확정은 초록, 미선발·포기는 붉은색으로만 표시된다.
- **지원자가 없는 마감 라운드** — 열 제목만 있는 빈 표가 떠서 불러오기에 실패한 것처럼
  보였다. "지원한 학생이 없습니다"로 알린다.
- **감사 기록 표의 열 너비** — 시각·행위자·행위 열이 좁아 글자가 옆 칸을 침범했다.
- **용어 통일** — 같은 값을 화면에 따라 "학번" 또는 "학생코드"로 부르고 있었다.
  엑셀·CSV 헤더와 같은 "학생코드"로 통일했다.
- **라운드 [결과] 탭의 기본 순위 보기** — 모집단위별 순위로 열려서, 대학별로 묶어 보여주는
  [지원 현황] 탭과 오갈 때 같은 학생의 순위가 다른 수로 보였다. 대학 전체 순위로 열린다.

### 화면 정리

- 프로그램 제목·설명과 제작자·버전이 **카드 한 장**으로 합쳐졌다. 제목을 지정하지 않은
  학교에서는 두 카드가 사실상 같은 말을 두 번 하고 있었다.
- 제목을 지정하지 않았을 때 사이드바 문구가 0.2.15 이전과 같아졌다.
- 라운드를 종료할 때 뜨는 "아직 입력을 확정하지 않은 학급이 있습니다" 경고가 붉은 상자로
  분리되어 눈에 띈다.

### 개발자용

- 담임 화면과 CSV 가 **같은 쿼리**(`fetch_teacher_results`)를 지난다. 따로 두면 필터 하나가
  어긋나는 순간 화면과 파일이 다른 명단을 보여주는데, 그 차이는 문자를 보낸 뒤에야 드러난다
- 0.2.13 이후 코드를 Fable 로 4회 감사(백엔드 / 자동 실행·트레이 / 프론트 / CSV 경로).
  치명 0건, 중대 2건(silent fallback 허용 목록 미등재, 레지스트리 권한 확대) 수정
- 회귀 테스트 30건 추가(총 837건). 새 방어선은 전부 변이 검사로 실효성 확인 — 그 과정에서
  헛도는 테스트를 하나 발견해 픽스처를 고쳤다

### 업그레이드 직후 달라지는 것

- 담임 화면의 결과 표에서 노란색(동점) 배경이 사라진다. 색이 선발 결과만 나타낸다
- 관리자 화면의 "학번" 표기가 "학생코드"로 바뀐다(값은 그대로)
- 라운드 [결과] 탭이 대학 전체 순위로 열린다. 모집단위별 순위는 버튼으로 전환한다

---

## 0.2.15

**프로그램에 학교 이름을 붙일 수 있다.** 인원 제한이 있는 전형과 없는 전형을 각각 다른
프로그램으로 운영할 때, 두 창을 구분할 수 없던 문제를 푼다
([#23](https://github.com/itmir913/principal-candidate-manager/issues/23),
[#30](https://github.com/itmir913/principal-candidate-manager/issues/30)).

스키마는 **동결 상태 그대로**다(`SCHEMA_VERSION = 1`, `migrations/` 무변경). 업그레이드해도
기존 DB에 마이그레이션이 돌지 않고, 구버전으로 되돌리는 경로도 살아 있다. API는 제목을
읽고 쓰는 경로 2개가 **추가**됐을 뿐 제거·개명은 없다.

### 새로 생긴 것

- **설정 탭** — 사이드바에 "설정"이 생겼다. 프로그램 제목과 설명을 지정하면 로그인 화면,
  사이드바, 브라우저 탭, 담임 화면에 표시된다. 입력하면서 로그인 화면이 어떻게 보일지
  바로 확인할 수 있다.
  - 제목 예: `○○고등학교 학교장추천` / 설명 예: `인원 제한 없는 대학`
  - 설명은 비워 둘 수 있다. 지정하기 전에는 지금까지와 같은 문구가 그대로 표시된다.
- **담임 화면에도 프로그램 이름 표시** — 학급 관리·지원자 등록·라운드 결과 세 화면 위에
  지금 접속한 프로그램이 무엇인지 표시된다. 담임도 두 프로그램을 구분할 수 있어야 한다.

### 수정

- **프로그램 두 개를 함께 쓸 때 자동 실행이 하나만 남던 문제** — 부팅 시 자동 실행 등록이
  마지막에 켠 프로그램으로 덮어써져, 재부팅하면 한 쪽만 자동으로 켜졌다. 이제 프로그램마다
  따로 등록된다. 업그레이드하면 기존 등록이 자동으로 옮겨진다.
- **작업 표시줄 아이콘 구분** — 아이콘에 마우스를 올리면 프로그램 이름과 포트 번호가 뜬다.
  (이 이름은 프로그램을 다시 시작한 뒤에 바뀐다.)
- **감사 기록의 빈 항목** — "프로그램 제목 변경"이 영문 그대로 표시되고 검색 목록에서도
  빠져 있었다.
- **미확정 학급 경고** — 라운드를 종료할 때 뜨는 "아직 입력을 확정하지 않은 학급이 있습니다"가
  안내문에 섞여 눈에 띄지 않았다. 붉은 경고 상자로 분리했다.

### 화면 정리

- 업데이트 & 백업 탭: 백업·복원을 맨 위로 올리고, 버전 정보 → 업데이트 방법 → 변경 내용 순
- About(제작자·라이선스)이 업데이트 & 백업 탭에서 설정 탭으로 옮겨졌다
- 감사 기록 메뉴가 설정·매뉴얼과 같은 아래 묶음으로 옮겨졌다
- 사이드바에서 긴 제목이 접기 버튼을 밀어내던 문제

### 개발자용

- `app_configs`에 `app_title`·`app_desc` 키 추가 — 범용 key-value 테이블이라 스키마 변경이 아니다
- 자동 실행 레지스트리 값 이름을 exe 경로 해시로 유일화(`paths::autostart_value_name`).
  레거시 `PCM` 항목은 **이 exe를 가리킬 때만** 정리한다 — 남의 등록을 지우지 않기 위해서다
- `AuditAction` 변형과 프론트 감사 라벨의 누락을 양방향으로 대조하는 테스트 추가.
  라벨 누락은 서버가 정상 동작하는 채로 화면에서만 드러나 그동안 아무도 몰랐다
- 회귀·방어 테스트 30건 추가, 새 방어선은 전부 변이 검사로 실효성 확인

### 업그레이드 직후 달라지는 것

- 학급 목록 엑셀 내보내기에 0학년 0반 행이 있었다면 더 이상 나오지 않는다(0.2.14의 후속)
- 자동 실행을 켜 둔 경우, 첫 실행 때 등록이 새 방식으로 옮겨진다. 프로그램을 두 개
  쓰고 있다면 **각각 한 번씩 실행해야** 둘 다 옮겨진다

---

## 0.2.14

졸업생 담당 계정의 **입력 마감이 아예 되지 않던 버그** 하나를 고친다
([#28](https://github.com/itmir913/principal-candidate-manager/issues/28)).

스키마는 **동결 상태 그대로**다(`SCHEMA_VERSION = 1`, `migrations/` 무변경).
업그레이드해도 기존 DB에 마이그레이션이 돌지 않고, 구버전으로 되돌리는 경로도 살아 있다.

### 수정

- **졸업생 담당의 입력 마감** — 졸업생 담당(0학년 0반)이 "입력 마감"을 누르면
  `FOREIGN KEY constraint failed` 오류로 실패했다. 졸업생 계정은 학급 목록에 실재하지
  않는 특수 계정인데, 확정 기록이 학급을 참조하고 있어서 생긴 문제다. 이제 정상 동작한다.
- **관리자 화면의 졸업생 확정 현황** — 졸업생 담당이 입력 마감을 해도 관리자의 라운드
  화면에는 흔적이 남지 않았다. 이제 학급 목록과 나란히 "졸업생 담당"으로 표시되고,
  미확정일 때는 미확정 목록에 함께 잡힌다.

### 개발자용

- `classes`의 0/0 sentinel 행이 진짜 학급으로 새지 않도록 차단 — 학급 목록·양식
  내보내기에서 제외, 삭제 거부, 재학생을 0학년 0반에 배정하는 입력 거부
- 회귀·방어 테스트 5건 추가, 전부 변이 검사로 실효성 확인
- 기존 테스트가 0/0 학급 행을 손수 만들어 두는 바람에 이 버그가 통과했다 —
  운영과 같은 전제에서 돌도록 고쳤다

### 업그레이드 직후 달라지는 것

- 학급 목록 엑셀 내보내기에 0학년 0반 행이 있었다면 더 이상 나오지 않는다
  (졸업생 담당은 학급이 아니라 별도 항목이다)

---

## 0.2.13

**채점 정합성 감사**(4라운드 + 독립 검증 2회)에서 나온 수정이다.

점수 계산·순위 산출 자체가 틀린 사례는 **발견되지 않았다** — 명세만 보고 재구현한
독립 오라클과 2,206행을 대조해 불일치 0건. 고친 것은 산출물 표기, 입력 관문,
그리고 "낡은 점수로 결정을 확정할 수 있던 경로"다.

스키마는 **동결 상태 그대로**다(`SCHEMA_VERSION = 1`, `migrations/` 무변경).
API 경로 102개도 그대로이고, 응답에 `needs_recalc` 필드가 **추가**됐을 뿐 제거·개명은 없다.

### 수정

- **마감 후 기초데이터 변경 감지** — 라운드를 마감한 뒤 기초데이터가 바뀌면 저장된
  총점이 낡는데 표시도 차단도 없었다. 이제 "재계산 필요"를 표시하고, 재계산 전에는
  추천 확정·자동 추천·라운드 마감을 막는다.
- **엑셀 라운드 열 라벨** — "정원 현황" 시트의 라운드 열이 배열 인덱스 기반이라,
  추천 0건 라운드가 빠지면 2차 수치가 "1차 추천" 열에 적혔다. 실제 차수를 따른다.
- **정원 범위 검증** — 대학·모집단위 정원을 API로 저장할 때 검증이 없어 0·음수 정원이
  저장되고 엑셀에 그대로 나갔다. Excel 설정 import와 같은 기준(1 이상 또는 무제한)으로 통일.
- **학생 번호 검증** — `seq_no`에 0·음수가 저장될 수 있었다. 단건 추가와 엑셀 업로드
  양쪽에서 1 이상을 요구한다.
- **정원 입력칸** — `0`을 조용히 `1`로 바꾸고 음수는 그대로 보냈다. 이제 값을 고치지 않고
  저장을 막고 안내를 띄운다.
- **동점 표식** — 모집단위 필터를 켠 채 대학 순위로 보면 동점 표식이 사라졌다.
  동점 상대가 다른 모집단위에 있어도 표식이 유지된다.

### 개발자용

- 독립 오라클(Python) 대조를 `npm run ci`에 통합 — 185시나리오 2,206행
- 회귀 테스트 30건 추가(총 794건), 전부 변이 검사로 실효성 확인
- CI가 백엔드·오라클·프론트 파생값을 한 번에 검증(`push`/`pull_request`)
- 의존성: `nanoid` 취약점 해소, 마이너·패치 일괄 상향(메이저 보류)

### ⚠ 업그레이드 직후 달라지는 것 — 반드시 읽어라

**1. 낡은 점수 상태면 추천·마감이 막힌다.**
라운드를 마감한 뒤 기초데이터를 다시 올린 적이 있는 설치는 업그레이드 즉시 추천 확정이
409, 라운드 마감이 422로 막힌다. 화면에 "재계산 필요" 배지가 뜬다.
**해소: 결과 탭에서 "점수 전체 재계산" 1회.** 고장이 아니라 의도된 차단이며, 해당 이력이
없는 설치는 아무 일도 일어나지 않는다.

**2. 0·음수 정원이 저장돼 있으면 그 대학·모집단위를 편집 저장할 수 없다.**
정원을 1 이상으로 고치거나 "무제한"을 선택하면 저장된다.

**3. 번호가 0인 학생이 든 명단은 재업로드가 거부된다.**
업로드는 전부 성공 아니면 전부 취소이므로 파일 전체가 422가 된다. 해당 행을 고쳐 다시 올린다.
(이미 저장된 학생은 그대로 남는다)

**4. 엑셀 "정원 현황"의 라운드 열 이름이 실제 차수를 따른다.**
이 파일의 헤더 문자열을 다른 도구로 읽고 있다면 확인이 필요하다.

### 배포 시점 주의

**선발 진행 중에는 올리지 마라.** 위 1번 때문에 관리자가 추천 확정을 못 하는 상황을
예고 없이 만난다. 라운드 사이나 마감 이후에 배포한다.
